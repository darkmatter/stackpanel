---
title: "Caddy utilities - pure functions that work with any Nix module system"
description: "Usage:"
---

## Usage

```nix
let caddyLib = import ./lib/caddy.nix { inherit pkgs lib; };
in caddyLib.mkCaddyScripts { ... }
```


All paths use $HOME at runtime for purity - no builtins.getEnv needed.
Override with CADDY_CONFIG_DIR environment variable if needed.

---
title: PerSystem Options
description: Configuration options for stackpanel.perSystem
---

# PerSystem Options

## `perSystem.aarch64-darwin.devenv.shells.default.stackpanel.apps`

Apps to assign ports and optionally create Caddy vhosts for.
Each app gets a port starting at basePort + 10.

| Property | Value |
|----------|-------|
| **Type** | `attribute set of (submodule)` |
| **Default** | `{ }` |

**Example:**

```nix
{
  web = {};
  server = { offset = 1; };
  docs = { domain = "docs"; };
  api = { domain = "api"; tls = true; };
}
```


---

## `perSystem.aarch64-darwin.devenv.shells.default.stackpanel.apps.<name>.domain`

Domain prefix for .localhost vhost.
If set, a Caddy vhost will be created at .localhost

| Property | Value |
|----------|-------|
| **Type** | `null or string` |
| **Default** | `null` |

**Example:**

`"api"`


---

## `perSystem.aarch64-darwin.devenv.shells.default.stackpanel.apps.<name>.offset`

Port offset from base port.
If null, offset is determined by position in apps attrset.

| Property | Value |
|----------|-------|
| **Type** | `null or signed integer` |
| **Default** | `null` |

**Example:**

`5`


---

## `perSystem.aarch64-darwin.devenv.shells.default.stackpanel.apps.<name>.tls`

Enable TLS for the vhost (requires Step CA)

| Property | Value |
|----------|-------|
| **Type** | `boolean` |
| **Default** | `false` |

---

## `perSystem.aarch64-darwin.devenv.shells.default.stackpanel.appsComputed`

Computed app configurations with ports and URLs

| Property | Value |
|----------|-------|
| **Type** | `attribute set of attribute set of unspecified value` |
| **Default** | _see below_ |
| **Read Only** | `true` |

**Default:**

```nix
{
  docs = {
    domain = "docs.localhost";
    offset = null;
    port = 6400;
    tls = false;
    url = "http://docs.localhost";
  };
  server = {
    domain = null;
    offset = null;
    port = 6401;
    tls = false;
    url = null;
  };
  web = {
    domain = "stackpanel.localhost";
    offset = null;
    port = 6402;
    tls = false;
    url = "http://stackpanel.localhost";
  };
}
```


---

## `perSystem.aarch64-darwin.devenv.shells.default.stackpanel.aws.certAuth.account-id`

AWS account ID

| Property | Value |
|----------|-------|
| **Type** | `string` |
| **Default** | `""` |

---

## `perSystem.aarch64-darwin.devenv.shells.default.stackpanel.aws.certAuth.cache-buffer-seconds`

Seconds before expiry to refresh cached credentials

| Property | Value |
|----------|-------|
| **Type** | `string` |
| **Default** | `"300"` |

---

## `perSystem.aarch64-darwin.devenv.shells.default.stackpanel.aws.certAuth.enable`

Whether to enable AWS Roles Anywhere cert auth.

| Property | Value |
|----------|-------|
| **Type** | `boolean` |
| **Default** | `false` |

**Example:**

`true`


---

## `perSystem.aarch64-darwin.devenv.shells.default.stackpanel.aws.certAuth.profile-arn`

AWS Roles Anywhere profile ARN

| Property | Value |
|----------|-------|
| **Type** | `string` |
| **Default** | `""` |

---

## `perSystem.aarch64-darwin.devenv.shells.default.stackpanel.aws.certAuth.prompt-on-shell`

Prompt for AWS cert-auth setup on shell entry if not configured

| Property | Value |
|----------|-------|
| **Type** | `boolean` |
| **Default** | `true` |

---

## `perSystem.aarch64-darwin.devenv.shells.default.stackpanel.aws.certAuth.region`

AWS region

| Property | Value |
|----------|-------|
| **Type** | `string` |
| **Default** | `"us-west-2"` |

---

## `perSystem.aarch64-darwin.devenv.shells.default.stackpanel.aws.certAuth.role-name`

IAM role name to assume

| Property | Value |
|----------|-------|
| **Type** | `string` |
| **Default** | `""` |

---

## `perSystem.aarch64-darwin.devenv.shells.default.stackpanel.aws.certAuth.trust-anchor-arn`

AWS Roles Anywhere trust anchor ARN

| Property | Value |
|----------|-------|
| **Type** | `string` |
| **Default** | `""` |

---

## `perSystem.aarch64-darwin.devenv.shells.default.stackpanel.caddy.auto-start`

Automatically start Caddy when entering the shell

| Property | Value |
|----------|-------|
| **Type** | `boolean` |
| **Default** | `false` |

---

## `perSystem.aarch64-darwin.devenv.shells.default.stackpanel.caddy.enable`

Whether to enable Caddy reverse proxy.

| Property | Value |
|----------|-------|
| **Type** | `boolean` |
| **Default** | `false` |

**Example:**

`true`


---

## `perSystem.aarch64-darwin.devenv.shells.default.stackpanel.caddy.project-name`

Project name used to compute a stable port. Set this to your project name for consistent port allocation.

| Property | Value |
|----------|-------|
| **Type** | `string` |
| **Default** | `"default"` |

**Example:**

`"myapp"`


---

## `perSystem.aarch64-darwin.devenv.shells.default.stackpanel.caddy.project-port`

Computed stable port based on project-name. Can be overridden manually.

| Property | Value |
|----------|-------|
| **Type** | `16 bit unsigned integer; between 0 and 65535 (both inclusive)` |
| **Default** | `caddyLib.mkProjectPort { name = cfg.project-name; }` |

**Example:**

`34521`


---

## `perSystem.aarch64-darwin.devenv.shells.default.stackpanel.caddy.use-step-tls`

Use Step CA for TLS certificates (requires stackpanel.network.step.enable)

| Property | Value |
|----------|-------|
| **Type** | `boolean` |
| **Default** | `false` |

---

## `perSystem.aarch64-darwin.devenv.shells.default.stackpanel.ci.enable`

Whether to enable CI/CD generation.

| Property | Value |
|----------|-------|
| **Type** | `boolean` |
| **Default** | `false` |

**Example:**

`true`


---

## `perSystem.aarch64-darwin.devenv.shells.default.stackpanel.ci.github.checks.branches`

This option has no description.

| Property | Value |
|----------|-------|
| **Type** | `list of string` |
| **Default** | _see below_ |

**Default:**

```nix
[
  "main"
]
```


---

## `perSystem.aarch64-darwin.devenv.shells.default.stackpanel.ci.github.checks.commands`

This option has no description.

| Property | Value |
|----------|-------|
| **Type** | `list of string` |
| **Default** | `[ ]` |

**Example:**

```nix
[
  "nix flake check"
  "nix build"
]
```


---

## `perSystem.aarch64-darwin.devenv.shells.default.stackpanel.ci.github.checks.enable`

Whether to enable standard CI checks workflow.

| Property | Value |
|----------|-------|
| **Type** | `boolean` |
| **Default** | `false` |

**Example:**

`true`


---

## `perSystem.aarch64-darwin.devenv.shells.default.stackpanel.ci.github.enable`

Whether to enable GitHub Actions.

| Property | Value |
|----------|-------|
| **Type** | `boolean` |
| **Default** | `false` |

**Example:**

`true`


---

## `perSystem.aarch64-darwin.devenv.shells.default.stackpanel.ci.github.workflows`

Workflow name -> workflow definition (raw)

| Property | Value |
|----------|-------|
| **Type** | `attribute set of (attribute set)` |
| **Default** | `{ }` |

---

## `perSystem.aarch64-darwin.devenv.shells.default.stackpanel.cli.enable`

Whether to enable CLI-based file generation.

| Property | Value |
|----------|-------|
| **Type** | `boolean` |
| **Default** | `true` |

**Example:**

`true`


---

## `perSystem.aarch64-darwin.devenv.shells.default.stackpanel.cli.quiet`

Suppress generation output messages

| Property | Value |
|----------|-------|
| **Type** | `boolean` |
| **Default** | `false` |

---

## `perSystem.aarch64-darwin.devenv.shells.default.stackpanel.data-dir`

Directory for stackpanel config (checked in)

| Property | Value |
|----------|-------|
| **Type** | `string` |
| **Default** | `".stackpanel"` |

---

## `perSystem.aarch64-darwin.devenv.shells.default.stackpanel.devenv.recommended.enable`

When enabled, applies recommended settings for stackpanel.devenv integration.

| Property | Value |
|----------|-------|
| **Type** | `boolean` |
| **Default** | `false` |

**Example:**

`true`


---

## `perSystem.aarch64-darwin.devenv.shells.default.stackpanel.devenv.recommended.formatters.enable`

When enabled, applies recommended settings for code formatters in the dev environment.

| Property | Value |
|----------|-------|
| **Type** | `boolean` |
| **Default** | `false` |

**Example:**

`true`


---

## `perSystem.aarch64-darwin.devenv.shells.default.stackpanel.direnv.hide-env-diff`

Hide the 'direnv: export +VAR...' log line (requires user's direnv.toml)

| Property | Value |
|----------|-------|
| **Type** | `boolean` |
| **Default** | `true` |

---

## `perSystem.aarch64-darwin.devenv.shells.default.stackpanel.enable`

Whether to enable stackpanel integration.

| Property | Value |
|----------|-------|
| **Type** | `boolean` |
| **Default** | `true` |

**Example:**

`true`


---

## `perSystem.aarch64-darwin.devenv.shells.default.stackpanel.gen-dir`

Directory for generated files (gitignored, regenerated on each devenv run)

| Property | Value |
|----------|-------|
| **Type** | `string` |
| **Default** | `".stackpanel/gen"` |

---

## `perSystem.aarch64-darwin.devenv.shells.default.stackpanel.globalServices.caddy.enable`

Whether to enable Global Caddy reverse proxy.

| Property | Value |
|----------|-------|
| **Type** | `boolean` |
| **Default** | `false` |

**Example:**

`true`


---

## `perSystem.aarch64-darwin.devenv.shells.default.stackpanel.globalServices.caddy.sites`

Sites to register with Caddy (domain -> upstream)

| Property | Value |
|----------|-------|
| **Type** | `attribute set of string` |
| **Default** | `{ }` |

**Example:**

```nix
{
  "api.localhost" = "localhost:8080";
  "myapp.localhost" = "localhost:3000";
}
```


---

## `perSystem.aarch64-darwin.devenv.shells.default.stackpanel.globalServices.enable`

Whether to enable Global singleton development services.

| Property | Value |
|----------|-------|
| **Type** | `boolean` |
| **Default** | `false` |

**Example:**

`true`


---

## `perSystem.aarch64-darwin.devenv.shells.default.stackpanel.globalServices.minio.console-port`

Minio console port. If null, uses computed port from stackpanel.ports.

| Property | Value |
|----------|-------|
| **Type** | `null or 16 bit unsigned integer; between 0 and 65535 (both inclusive)` |
| **Default** | `null` |

---

## `perSystem.aarch64-darwin.devenv.shells.default.stackpanel.globalServices.minio.enable`

Whether to enable Global Minio (S3) service.

| Property | Value |
|----------|-------|
| **Type** | `boolean` |
| **Default** | `false` |

**Example:**

`true`


---

## `perSystem.aarch64-darwin.devenv.shells.default.stackpanel.globalServices.minio.package`

Minio package to use

| Property | Value |
|----------|-------|
| **Type** | `package` |
| **Default** | `<derivation minio-2025-10-15T17-29-55Z>` |

---

## `perSystem.aarch64-darwin.devenv.shells.default.stackpanel.globalServices.minio.port`

Minio API port. If null, uses computed port from stackpanel.ports.

| Property | Value |
|----------|-------|
| **Type** | `null or 16 bit unsigned integer; between 0 and 65535 (both inclusive)` |
| **Default** | `null` |

---

## `perSystem.aarch64-darwin.devenv.shells.default.stackpanel.globalServices.postgres.databases`

List of databases to create for this project

| Property | Value |
|----------|-------|
| **Type** | `list of string` |
| **Default** | _see below_ |

**Default:**

```nix
[
  "stackpanel"
]
```


**Example:**

```nix
[
  "myapp"
  "myapp_test"
]
```


---

## `perSystem.aarch64-darwin.devenv.shells.default.stackpanel.globalServices.postgres.enable`

Whether to enable Global PostgreSQL service.

| Property | Value |
|----------|-------|
| **Type** | `boolean` |
| **Default** | `false` |

**Example:**

`true`


---

## `perSystem.aarch64-darwin.devenv.shells.default.stackpanel.globalServices.postgres.package`

PostgreSQL package to use

| Property | Value |
|----------|-------|
| **Type** | `package` |
| **Default** | `<derivation postgresql-17.7>` |

---

## `perSystem.aarch64-darwin.devenv.shells.default.stackpanel.globalServices.postgres.port`

PostgreSQL port. If null, uses computed port from stackpanel.ports.

| Property | Value |
|----------|-------|
| **Type** | `null or 16 bit unsigned integer; between 0 and 65535 (both inclusive)` |
| **Default** | `null` |

---

## `perSystem.aarch64-darwin.devenv.shells.default.stackpanel.globalServices.project-name`

Project name used for registering databases and sites

| Property | Value |
|----------|-------|
| **Type** | `string` |
| **Default** | `"default"` |

**Example:**

`"stackpanel"`


---

## `perSystem.aarch64-darwin.devenv.shells.default.stackpanel.globalServices.redis.enable`

Whether to enable Global Redis service.

| Property | Value |
|----------|-------|
| **Type** | `boolean` |
| **Default** | `false` |

**Example:**

`true`


---

## `perSystem.aarch64-darwin.devenv.shells.default.stackpanel.globalServices.redis.package`

Redis package to use

| Property | Value |
|----------|-------|
| **Type** | `package` |
| **Default** | `<derivation redis-8.2.2>` |

---

## `perSystem.aarch64-darwin.devenv.shells.default.stackpanel.globalServices.redis.port`

Redis port. If null, uses computed port from stackpanel.ports.

| Property | Value |
|----------|-------|
| **Type** | `null or 16 bit unsigned integer; between 0 and 65535 (both inclusive)` |
| **Default** | `null` |

---

## `perSystem.aarch64-darwin.devenv.shells.default.stackpanel.ide.cursor.enable`

Whether to enable Cursor editor integration (not yet implemented).

| Property | Value |
|----------|-------|
| **Type** | `boolean` |
| **Default** | `false` |

**Example:**

`true`


---

## `perSystem.aarch64-darwin.devenv.shells.default.stackpanel.ide.enable`

Generate IDE configuration files into .stackpanel/gen/ide/

| Property | Value |
|----------|-------|
| **Type** | `boolean` |
| **Default** | `false` |

**Example:**

`true`


---

## `perSystem.aarch64-darwin.devenv.shells.default.stackpanel.ide.vscode.enable`

Generate VS Code workspace and configuration files

| Property | Value |
|----------|-------|
| **Type** | `boolean` |
| **Default** | `false` |

**Example:**

`true`


---

## `perSystem.aarch64-darwin.devenv.shells.default.stackpanel.ide.vscode.existing-settings-path`

Path to existing VS Code settings.json to merge with generated settings.

WARNING: This creates IMPURE evaluation - the file must exist at Nix evaluation time.

| Property | Value |
|----------|-------|
| **Type** | `null or absolute path` |
| **Default** | `null` |

---

## `perSystem.aarch64-darwin.devenv.shells.default.stackpanel.ide.vscode.extensions`

Recommended VS Code extension IDs

| Property | Value |
|----------|-------|
| **Type** | `list of string` |
| **Default** | `[ ]` |

---

## `perSystem.aarch64-darwin.devenv.shells.default.stackpanel.ide.vscode.extra-folders`

Additional workspace folders to include

| Property | Value |
|----------|-------|
| **Type** | `list of attribute set of string` |
| **Default** | `[ ]` |

---

## `perSystem.aarch64-darwin.devenv.shells.default.stackpanel.ide.vscode.output-mode`

Where to output VS Code settings:
- "workspace": Generate .stackpanel/gen/ide/vscode/stackpanel.code-workspace (default, safe)
- "settingsJson": Generate .vscode/settings.json (CAUTION: may overwrite existing file)

| Property | Value |
|----------|-------|
| **Type** | `one of "workspace", "settingsJson"` |
| **Default** | `"workspace"` |

---

## `perSystem.aarch64-darwin.devenv.shells.default.stackpanel.ide.vscode.settings`

VS Code settings to include in the generated configuration.
These take highest priority and will override any existing or generated settings.

| Property | Value |
|----------|-------|
| **Type** | `attribute set of anything` |
| **Default** | `{ }` |

---

## `perSystem.aarch64-darwin.devenv.shells.default.stackpanel.ide.vscode.workspace-name`

Name for the generated .code-workspace file (without extension)

| Property | Value |
|----------|-------|
| **Type** | `string` |
| **Default** | `"stackpanel"` |

---

## `perSystem.aarch64-darwin.devenv.shells.default.stackpanel.ide.zed.enable`

Whether to enable Zed editor integration (not yet implemented).

| Property | Value |
|----------|-------|
| **Type** | `boolean` |
| **Default** | `false` |

**Example:**

`true`


---

## `perSystem.aarch64-darwin.devenv.shells.default.stackpanel.motd.commands`

List of available commands to show in MOTD

| Property | Value |
|----------|-------|
| **Type** | `list of (submodule)` |
| **Default** | `[ ]` |

---

## `perSystem.aarch64-darwin.devenv.shells.default.stackpanel.motd.commands.*.description`

Command description

| Property | Value |
|----------|-------|
| **Type** | `string` |
| **Default** | _none_ |

---

## `perSystem.aarch64-darwin.devenv.shells.default.stackpanel.motd.commands.*.name`

Command name

| Property | Value |
|----------|-------|
| **Type** | `string` |
| **Default** | _none_ |

---

## `perSystem.aarch64-darwin.devenv.shells.default.stackpanel.motd.enable`

Show MOTD with help text on shell entry

| Property | Value |
|----------|-------|
| **Type** | `boolean` |
| **Default** | `true` |

---

## `perSystem.aarch64-darwin.devenv.shells.default.stackpanel.motd.features`

List of enabled features to show in MOTD

| Property | Value |
|----------|-------|
| **Type** | `list of string` |
| **Default** | `[ ]` |

---

## `perSystem.aarch64-darwin.devenv.shells.default.stackpanel.motd.hints`

List of hints to show in MOTD

| Property | Value |
|----------|-------|
| **Type** | `list of string` |
| **Default** | `[ ]` |

---

## `perSystem.aarch64-darwin.devenv.shells.default.stackpanel.network.step.ca-fingerprint`

Step CA root certificate fingerprint

| Property | Value |
|----------|-------|
| **Type** | `string` |
| **Default** | `""` |

---

## `perSystem.aarch64-darwin.devenv.shells.default.stackpanel.network.step.ca-url`

Step CA URL (e.g., https://ca.internal:443)

| Property | Value |
|----------|-------|
| **Type** | `string` |
| **Default** | `""` |

---

## `perSystem.aarch64-darwin.devenv.shells.default.stackpanel.network.step.cert-name`

Common name for the device certificate

| Property | Value |
|----------|-------|
| **Type** | `string` |
| **Default** | `"device"` |

---

## `perSystem.aarch64-darwin.devenv.shells.default.stackpanel.network.step.enable`

Whether to enable Step CA certificate management.

| Property | Value |
|----------|-------|
| **Type** | `boolean` |
| **Default** | `false` |

**Example:**

`true`


---

## `perSystem.aarch64-darwin.devenv.shells.default.stackpanel.network.step.prompt-on-shell`

Prompt for certificate setup on shell entry if not configured

| Property | Value |
|----------|-------|
| **Type** | `boolean` |
| **Default** | `true` |

---

## `perSystem.aarch64-darwin.devenv.shells.default.stackpanel.network.step.provisioner`

Step CA provisioner name

| Property | Value |
|----------|-------|
| **Type** | `string` |
| **Default** | `"Authentik"` |

---

## `perSystem.aarch64-darwin.devenv.shells.default.stackpanel.ports.base-port`

The computed base port for this project

| Property | Value |
|----------|-------|
| **Type** | `16 bit unsigned integer; between 0 and 65535 (both inclusive)` |
| **Default** | `6400` |
| **Read Only** | `true` |

---

## `perSystem.aarch64-darwin.devenv.shells.default.stackpanel.ports.enable`

Whether to enable Automatic port assignment.

| Property | Value |
|----------|-------|
| **Type** | `boolean` |
| **Default** | `true` |

**Example:**

`true`


---

## `perSystem.aarch64-darwin.devenv.shells.default.stackpanel.ports.min-port`

Minimum port number for the port range

| Property | Value |
|----------|-------|
| **Type** | `16 bit unsigned integer; between 0 and 65535 (both inclusive)` |
| **Default** | `3000` |

---

## `perSystem.aarch64-darwin.devenv.shells.default.stackpanel.ports.modulus`

Port rounding modulus. Ports are rounded to nearest multiple.
Use 100 for ports like 3100, 3200, 3300.
Use 10 for ports like 3110, 3120, 3130 (if collision occurs).

| Property | Value |
|----------|-------|
| **Type** | `signed integer` |
| **Default** | `100` |

**Example:**

`10`


---

## `perSystem.aarch64-darwin.devenv.shells.default.stackpanel.ports.port-range`

Range of ports to use (min-port to min-port + port-range)

| Property | Value |
|----------|-------|
| **Type** | `signed integer` |
| **Default** | `7000` |

---

## `perSystem.aarch64-darwin.devenv.shells.default.stackpanel.ports.project-name`

Project name used for port computation. Set this to match your project name.

| Property | Value |
|----------|-------|
| **Type** | `string` |
| **Default** | `"default"` |

**Example:**

`"myapp"`


---

## `perSystem.aarch64-darwin.devenv.shells.default.stackpanel.ports.service`

Computed service information by key.
Access: config.stackpanel.ports.service.POSTGRES.port

| Property | Value |
|----------|-------|
| **Type** | `attribute set of attribute set of unspecified value` |
| **Default** | _see below_ |
| **Read Only** | `true` |

**Default:**

```nix
{
  MINIO = {
    displayName = "Minio";
    key = "MINIO";
    name = "Minio";
    port = 6412;
  };
  MINIO_CONSOLE = {
    displayName = "Minio Console";
    key = "MINIO_CONSOLE";
    name = "Minio Console";
    port = 6413;
  };
  POSTGRES = {
    displayName = "PostgreSQL";
    key = "POSTGRES";
    name = "PostgreSQL";
    port = 6410;
  };
  REDIS = {
    displayName = "Redis";
    key = "REDIS";
    name = "Redis";
    port = 6411;
  };
}
```


---

## `perSystem.aarch64-darwin.devenv.shells.default.stackpanel.ports.services`

List of infrastructure services that need ports.
Each service gets a port at basePort + 10 + index.
Environment variable: STACKPANEL__PORT

| Property | Value |
|----------|-------|
| **Type** | `list of (submodule)` |
| **Default** | `[ ]` |

**Example:**

```nix
[
  { key = "POSTGRES"; name = "PostgreSQL"; }
  { key = "REDIS"; name = "Redis"; }
  { key = "MINIO"; name = "Minio"; }
  { key = "MINIO_CONSOLE"; name = "Minio Console"; }
]
```


---

## `perSystem.aarch64-darwin.devenv.shells.default.stackpanel.ports.services.*.key`

Unique key for the service (used in env var: STACKPANEL__PORT)

| Property | Value |
|----------|-------|
| **Type** | `string` |
| **Default** | _none_ |

**Example:**

`"POSTGRES"`


---

## `perSystem.aarch64-darwin.devenv.shells.default.stackpanel.ports.services.*.name`

Human-readable name for display

| Property | Value |
|----------|-------|
| **Type** | `string` |
| **Default** | `""` |

**Example:**

`"PostgreSQL"`


---

## `perSystem.aarch64-darwin.devenv.shells.default.stackpanel.secrets.apps`

Per-app secrets configuration (auto-discovered from .stackpanel/secrets/apps/)

| Property | Value |
|----------|-------|
| **Type** | `attribute set of (submodule)` |
| **Default** | _see below_ |

**Default:**

```nix
{
  api = {
    appConfig = { };
    commonSchema = { };
    environments = {
      dev = {
        extraKeys = [ ];
        schema = { };
        users = [ ];
      };
      prod = {
        extraKeys = [ ];
        schema = { };
        users = [ ];
      };
      staging = {
        extraKeys = [ ];
        schema = { };
        users = [ ];
      };
    };
  };
}
```


---

## `perSystem.aarch64-darwin.devenv.shells.default.stackpanel.secrets.apps.<name>.app-config`

App-specific configuration (codegen settings)

| Property | Value |
|----------|-------|
| **Type** | `attribute set` |
| **Default** | `{ }` |

---

## `perSystem.aarch64-darwin.devenv.shells.default.stackpanel.secrets.apps.<name>.common-schema`

Schema shared across all environments

| Property | Value |
|----------|-------|
| **Type** | `attribute set` |
| **Default** | `{ }` |

---

## `perSystem.aarch64-darwin.devenv.shells.default.stackpanel.secrets.apps.<name>.environments`

Per-environment configurations

| Property | Value |
|----------|-------|
| **Type** | `attribute set of (submodule)` |
| **Default** | `{ }` |

---

## `perSystem.aarch64-darwin.devenv.shells.default.stackpanel.secrets.apps.<name>.environments.<name>.extra-keys`

Additional AGE keys (CI systems, servers)

| Property | Value |
|----------|-------|
| **Type** | `list of string` |
| **Default** | `[ ]` |

---

## `perSystem.aarch64-darwin.devenv.shells.default.stackpanel.secrets.apps.<name>.environments.<name>.schema`

Environment-specific schema (overrides common)

| Property | Value |
|----------|-------|
| **Type** | `attribute set` |
| **Default** | `{ }` |

---

## `perSystem.aarch64-darwin.devenv.shells.default.stackpanel.secrets.apps.<name>.environments.<name>.users`

User names who can access this environment's secrets

| Property | Value |
|----------|-------|
| **Type** | `list of string` |
| **Default** | `[ ]` |

---

## `perSystem.aarch64-darwin.devenv.shells.default.stackpanel.secrets.backend`

Backend for secret resolution.
- "vals" (recommended): Multi-backend secret resolver supporting SOPS, AWS Secrets Manager, 1Password, Vault, Doppler
- "sops": Direct SOPS usage only
vals is backwards-compatible with SOPS files via ref+sops:// URIs

| Property | Value |
|----------|-------|
| **Type** | `one of "vals", "sops"` |
| **Default** | `"vals"` |

---

## `perSystem.aarch64-darwin.devenv.shells.default.stackpanel.secrets.default-environments`

Default environments for all apps

| Property | Value |
|----------|-------|
| **Type** | `list of string` |
| **Default** | _see below_ |

**Default:**

```nix
[
  "dev"
  "staging"
  "prod"
]
```


---

## `perSystem.aarch64-darwin.devenv.shells.default.stackpanel.secrets.enable`

Whether to enable SOPS/vals secrets management.

| Property | Value |
|----------|-------|
| **Type** | `boolean` |
| **Default** | `false` |

**Example:**

`true`


---

## `perSystem.aarch64-darwin.devenv.shells.default.stackpanel.secrets.generate-placeholders`

Generate placeholder .yaml files for each app/environment

| Property | Value |
|----------|-------|
| **Type** | `boolean` |
| **Default** | `true` |

---

## `perSystem.aarch64-darwin.devenv.shells.default.stackpanel.secrets.secrets-dir`

Directory for encrypted secrets files

| Property | Value |
|----------|-------|
| **Type** | `string` |
| **Default** | `"secrets"` |

---

## `perSystem.aarch64-darwin.devenv.shells.default.stackpanel.secrets.users`

Team members and their AGE public keys

| Property | Value |
|----------|-------|
| **Type** | `attribute set of (submodule)` |
| **Default** | `{ }` |

**Example:**

```nix
{
  alice = { pubkey = "age1..."; github = "alice"; admin = true; };
  bob = { pubkey = "age1..."; github = "bobdev"; };
}
```


---

## `perSystem.aarch64-darwin.devenv.shells.default.stackpanel.secrets.users.<name>.admin`

Admin users can decrypt all secrets

| Property | Value |
|----------|-------|
| **Type** | `boolean` |
| **Default** | `false` |

---

## `perSystem.aarch64-darwin.devenv.shells.default.stackpanel.secrets.users.<name>.github`

GitHub username (for display/lookup)

| Property | Value |
|----------|-------|
| **Type** | `string` |
| **Default** | `""` |

---

## `perSystem.aarch64-darwin.devenv.shells.default.stackpanel.secrets.users.<name>.pubkey`

AGE public key (age1...) or SSH public key

| Property | Value |
|----------|-------|
| **Type** | `string` |
| **Default** | _none_ |

---

## `perSystem.aarch64-darwin.devenv.shells.default.stackpanel.state-dir`

Directory for runtime state (gitignored)

| Property | Value |
|----------|-------|
| **Type** | `string` |
| **Default** | `".stackpanel/state"` |

---

## `perSystem.aarch64-darwin.devenv.shells.default.stackpanel.state.enable`

Whether to enable state file generation.

| Property | Value |
|----------|-------|
| **Type** | `boolean` |
| **Default** | `true` |

**Example:**

`true`


---

## `perSystem.aarch64-darwin.devenv.shells.default.stackpanel.state.file`

Name of the state file in stateDir

| Property | Value |
|----------|-------|
| **Type** | `string` |
| **Default** | `"stackpanel.json"` |

---

## `perSystem.aarch64-darwin.devenv.shells.default.stackpanel.theme.config-file`

Custom starship.toml config file (uses stackpanel default if not set)

| Property | Value |
|----------|-------|
| **Type** | `null or absolute path` |
| **Default** | `null` |

---

## `perSystem.aarch64-darwin.devenv.shells.default.stackpanel.theme.enable`

Whether to enable Starship prompt for stackpanel devenv.

| Property | Value |
|----------|-------|
| **Type** | `boolean` |
| **Default** | `false` |

**Example:**

`true`


---


---
title: Secrets Options
description: Configuration options for stackpanel.secrets
---

# Secrets Options

## `stackpanel.secrets.apps`

Per-app secrets configuration (auto-discovered from .stackpanel/secrets/apps/)

| Property | Value |
|----------|-------|
| **Type** | `attribute set of (submodule)` |
| **Default** | `{ }` |


---

## `stackpanel.secrets.apps.<name>.appConfig`

App-specific configuration (codegen settings)

| Property | Value |
|----------|-------|
| **Type** | `attribute set` |
| **Default** | `{ }` |


---

## `stackpanel.secrets.apps.<name>.commonSchema`

Schema shared across all environments

| Property | Value |
|----------|-------|
| **Type** | `attribute set` |
| **Default** | `{ }` |


---

## `stackpanel.secrets.apps.<name>.environments`

Per-environment configurations

| Property | Value |
|----------|-------|
| **Type** | `attribute set of (submodule)` |
| **Default** | `{ }` |


---

## `stackpanel.secrets.apps.<name>.environments.<name>.extraKeys`

Additional AGE keys (CI systems, servers)

| Property | Value |
|----------|-------|
| **Type** | `list of string` |
| **Default** | `[ ]` |


---

## `stackpanel.secrets.apps.<name>.environments.<name>.schema`

Environment-specific schema (overrides common)

| Property | Value |
|----------|-------|
| **Type** | `attribute set` |
| **Default** | `{ }` |


---

## `stackpanel.secrets.apps.<name>.environments.<name>.users`

User names who can access this environment's secrets

| Property | Value |
|----------|-------|
| **Type** | `list of string` |
| **Default** | `[ ]` |


---

## `stackpanel.secrets.backend`

Backend for secret resolution.
- "vals" (recommended): Multi-backend secret resolver supporting SOPS, AWS Secrets Manager, 1Password, Vault, Doppler
- "sops": Direct SOPS usage only
vals is backwards-compatible with SOPS files via ref+sops:// URIs

| Property | Value |
|----------|-------|
| **Type** | `one of "vals", "sops"` |
| **Default** | `"vals"` |


---

## `stackpanel.secrets.defaultEnvironments`

Default environments for all apps

| Property | Value |
|----------|-------|
| **Type** | `list of string` |
| **Default** | _see below_ |


**Default:**

```nix
[
  "dev"
  "staging"
  "prod"
]
```


---

## `stackpanel.secrets.enable`

Whether to enable SOPS/vals secrets management.

| Property | Value |
|----------|-------|
| **Type** | `boolean` |
| **Default** | `false` |


**Example:**

`true`


---

## `stackpanel.secrets.generatePlaceholders`

Generate placeholder .yaml files for each app/environment

| Property | Value |
|----------|-------|
| **Type** | `boolean` |
| **Default** | `true` |


---

## `stackpanel.secrets.secretsDir`

Directory for encrypted secrets files

| Property | Value |
|----------|-------|
| **Type** | `string` |
| **Default** | `"secrets"` |


---

## `stackpanel.secrets.users`

Team members and their AGE public keys

| Property | Value |
|----------|-------|
| **Type** | `attribute set of (submodule)` |
| **Default** | `{ }` |


**Example:**

```nix
{
  alice = { pubkey = "age1..."; github = "alice"; admin = true; };
  bob = { pubkey = "age1..."; github = "bobdev"; };
}
```


---

## `stackpanel.secrets.users.<name>.admin`

Admin users can decrypt all secrets

| Property | Value |
|----------|-------|
| **Type** | `boolean` |
| **Default** | `false` |


---

## `stackpanel.secrets.users.<name>.github`

GitHub username (for display/lookup)

| Property | Value |
|----------|-------|
| **Type** | `string` |
| **Default** | `""` |


---

## `stackpanel.secrets.users.<name>.pubkey`

AGE public key (age1...) or SSH public key

| Property | Value |
|----------|-------|
| **Type** | `string` |
| **Default** | _none_ |


---


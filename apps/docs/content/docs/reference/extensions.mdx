---
title: extensions
description: Configuration options for stackpanel.extensions
icon: Settings
---

# Extensions Options

## `extensions`

Extensions that provide features and UI panels for stackpanel.

Extensions are feature modules that compose core stackpanel features:
  - File generation (stackpanel.files.entries)
  - Script generation (stackpanel.scripts)
  - Tasks (stackpanel.tasks)
  - Variables/secrets (stackpanel.secrets)
  - Shell hooks (stackpanel.devshell.hooks)
  - UI panels (stackpanel.extensions.*.panels)

Each extension can define:
  - `name`: Display name
  - `description`: What the extension does
  - `enabled`: Whether the extension is active
  - `builtin`: Whether it's shipped with stackpanel
  - `features`: Which core features it uses
  - `panels`: List of UI panels to render
  - `apps`: Per-app computed data

Builtin extensions (like SST) register themselves automatically.
External extensions can be added from GitHub or local paths.

| Property | Value |
|----------|-------|
| **Type** | `attribute set of (submodule)` |
| **Default** | `{ }` |

**Example:**

```nix
{
  sst = {
    name = "SST Infrastructure";
    enabled = true;
    builtin = true;
    category = "EXTENSION_CATEGORY_INFRASTRUCTURE";
    features = {
      files = true;
      scripts = true;
      secrets = true;
    };
    panels = [
      {
        id = "sst-status";
        title = "SST Infrastructure";
        type = "PANEL_TYPE_STATUS";
        fields = [
          { name = "metrics"; type = "FIELD_TYPE_STRING"; value = "[...]"; }
        ];
      }
    ];
  };

  my-extension = {
    name = "My Custom Extension";
    enabled = true;
    source = {
      type = "EXTENSION_SOURCE_TYPE_GITHUB";
      repo = "myorg/my-stackpanel-extension";
      ref = "main";
    };
  };
}
```


---

## `extensions.<name>.apps`

Per-app extension data (app name -\> extension data)

| Property | Value |
|----------|-------|
| **Type** | `attribute set of (submodule)` |
| **Default** | `{ }` |

---

## `extensions.<name>.apps.<name>.config`

Extension config for this app (string key-value pairs)

| Property | Value |
|----------|-------|
| **Type** | `attribute set of string` |
| **Default** | `{ }` |

---

## `extensions.<name>.apps.<name>.enabled`

Whether extension is enabled for this app

| Property | Value |
|----------|-------|
| **Type** | `boolean` |
| **Default** | `true` |

---

## `extensions.<name>.builtin`

Whether this is a built-in extension shipped with stackpanel

| Property | Value |
|----------|-------|
| **Type** | `boolean` |
| **Default** | `false` |

---

## `extensions.<name>.category`

Category for grouping in UI

| Property | Value |
|----------|-------|
| **Type** | `one of "EXTENSION_CATEGORY_UNSPECIFIED", "EXTENSION_CATEGORY_INFRASTRUCTURE", "EXTENSION_CATEGORY_CI_CD", "EXTENSION_CATEGORY_DATABASE", "EXTENSION_CATEGORY_SECRETS", "EXTENSION_CATEGORY_DEPLOYMENT", "EXTENSION_CATEGORY_DEVELOPMENT", "EXTENSION_CATEGORY_MONITORING", "EXTENSION_CATEGORY_INTEGRATION"` |
| **Default** | `"EXTENSION_CATEGORY_UNSPECIFIED"` |

---

## `extensions.<name>.dependencies`

Other extensions this depends on

| Property | Value |
|----------|-------|
| **Type** | `list of string` |
| **Default** | `[ ]` |

---

## `extensions.<name>.description`

Human-readable description of what the extension does

| Property | Value |
|----------|-------|
| **Type** | `null or string` |
| **Default** | `null` |

---

## `extensions.<name>.enabled`

Whether this extension is enabled

| Property | Value |
|----------|-------|
| **Type** | `boolean` |
| **Default** | `true` |

---

## `extensions.<name>.features`

Which core stackpanel features this extension configures

| Property | Value |
|----------|-------|
| **Type** | `submodule` |
| **Default** | `{ }` |

---

## `extensions.<name>.features.checks`

Extension defines checks/validations

| Property | Value |
|----------|-------|
| **Type** | `boolean` |
| **Default** | `false` |

---

## `extensions.<name>.features.files`

Extension generates files via stackpanel.files

| Property | Value |
|----------|-------|
| **Type** | `boolean` |
| **Default** | `false` |

---

## `extensions.<name>.features.packages`

Extension adds devshell packages

| Property | Value |
|----------|-------|
| **Type** | `boolean` |
| **Default** | `false` |

---

## `extensions.<name>.features.scripts`

Extension provides shell scripts/commands

| Property | Value |
|----------|-------|
| **Type** | `boolean` |
| **Default** | `false` |

---

## `extensions.<name>.features.secrets`

Extension manages secrets/variables

| Property | Value |
|----------|-------|
| **Type** | `boolean` |
| **Default** | `false` |

---

## `extensions.<name>.features.services`

Extension configures services/processes

| Property | Value |
|----------|-------|
| **Type** | `boolean` |
| **Default** | `false` |

---

## `extensions.<name>.features.shell-hooks`

Extension adds shell hooks

| Property | Value |
|----------|-------|
| **Type** | `boolean` |
| **Default** | `false` |

---

## `extensions.<name>.features.tasks`

Extension defines tasks

| Property | Value |
|----------|-------|
| **Type** | `boolean` |
| **Default** | `false` |

---

## `extensions.<name>.name`

Display name of the extension

| Property | Value |
|----------|-------|
| **Type** | `string` |
| **Default** | _none_ |

---

## `extensions.<name>.panels`

UI panels provided by this extension

| Property | Value |
|----------|-------|
| **Type** | `list of (submodule)` |
| **Default** | `[ ]` |

---

## `extensions.<name>.panels.*.description`

Panel description

| Property | Value |
|----------|-------|
| **Type** | `null or string` |
| **Default** | `null` |

---

## `extensions.<name>.panels.*.fields`

Panel configuration fields

| Property | Value |
|----------|-------|
| **Type** | `list of (submodule)` |
| **Default** | `[ ]` |

---

## `extensions.<name>.panels.*.fields.*.name`

Field name (maps to component prop)

| Property | Value |
|----------|-------|
| **Type** | `string` |
| **Default** | _none_ |

---

## `extensions.<name>.panels.*.fields.*.options`

Options for select fields

| Property | Value |
|----------|-------|
| **Type** | `list of string` |
| **Default** | `[ ]` |

---

## `extensions.<name>.panels.*.fields.*.type`

Field type

| Property | Value |
|----------|-------|
| **Type** | `one of "FIELD_TYPE_UNSPECIFIED", "FIELD_TYPE_STRING", "FIELD_TYPE_NUMBER", "FIELD_TYPE_BOOLEAN", "FIELD_TYPE_SELECT", "FIELD_TYPE_MULTISELECT", "FIELD_TYPE_APP_FILTER", "FIELD_TYPE_COLUMNS", "FIELD_TYPE_JSON"` |
| **Default** | `"FIELD_TYPE_STRING"` |

---

## `extensions.<name>.panels.*.fields.*.value`

Field value (JSON-encoded for complex types)

| Property | Value |
|----------|-------|
| **Type** | `string` |
| **Default** | `""` |

---

## `extensions.<name>.panels.*.id`

Unique panel identifier

| Property | Value |
|----------|-------|
| **Type** | `string` |
| **Default** | _none_ |

---

## `extensions.<name>.panels.*.order`

Display order (lower = first)

| Property | Value |
|----------|-------|
| **Type** | `signed integer` |
| **Default** | `100` |

---

## `extensions.<name>.panels.*.title`

Display title

| Property | Value |
|----------|-------|
| **Type** | `string` |
| **Default** | _none_ |

---

## `extensions.<name>.panels.*.type`

Panel type (determines which component to render)

| Property | Value |
|----------|-------|
| **Type** | `one of "PANEL_TYPE_UNSPECIFIED", "PANEL_TYPE_STATUS", "PANEL_TYPE_APPS_GRID", "PANEL_TYPE_FORM", "PANEL_TYPE_TABLE", "PANEL_TYPE_CUSTOM"` |
| **Default** | `"PANEL_TYPE_STATUS"` |

---

## `extensions.<name>.priority`

Load order priority (lower = earlier)

| Property | Value |
|----------|-------|
| **Type** | `signed integer` |
| **Default** | `100` |

---

## `extensions.<name>.source`

Extension source configuration

| Property | Value |
|----------|-------|
| **Type** | `submodule` |
| **Default** | `{ }` |

---

## `extensions.<name>.source.module-path`

Path to the Nix module within the source

| Property | Value |
|----------|-------|
| **Type** | `null or string` |
| **Default** | `null` |

---

## `extensions.<name>.source.package`

NPM package name for npm source type

| Property | Value |
|----------|-------|
| **Type** | `null or string` |
| **Default** | `null` |

---

## `extensions.<name>.source.path`

Local path for local source type

| Property | Value |
|----------|-------|
| **Type** | `null or string` |
| **Default** | `null` |

---

## `extensions.<name>.source.ref`

Git ref (branch, tag, commit) for github source type

| Property | Value |
|----------|-------|
| **Type** | `null or string` |
| **Default** | `null` |

---

## `extensions.<name>.source.repo`

GitHub repository (owner/repo) for github source type

| Property | Value |
|----------|-------|
| **Type** | `null or string` |
| **Default** | `null` |

---

## `extensions.<name>.source.type`

Source type for the extension

| Property | Value |
|----------|-------|
| **Type** | `one of "EXTENSION_SOURCE_TYPE_UNSPECIFIED", "EXTENSION_SOURCE_TYPE_BUILTIN", "EXTENSION_SOURCE_TYPE_LOCAL", "EXTENSION_SOURCE_TYPE_GITHUB", "EXTENSION_SOURCE_TYPE_NPM", "EXTENSION_SOURCE_TYPE_URL"` |
| **Default** | `"EXTENSION_SOURCE_TYPE_BUILTIN"` |

---

## `extensions.<name>.source.url`

URL for url source type

| Property | Value |
|----------|-------|
| **Type** | `null or string` |
| **Default** | `null` |

---

## `extensions.<name>.srcDir`

Path to extension's src/ directory for scripts, checks, and files.

When specified, the extension system will auto-discover resources:
  - src/scripts/*.sh -\> stackpanel.scripts.\<extName\>:\<scriptName\>
  - src/checks/*.sh -\> stackpanel.healthchecks.\<extName\>:\<checkName\>
  - src/files/* -\> available for stackpanel.files.entries

Resources are automatically namespaced with the extension name.
Explicit Nix definitions take priority over auto-discovered resources.

Example:
  srcDir = ./src;  # Relative to extension module

| Property | Value |
|----------|-------|
| **Type** | `null or absolute path` |
| **Default** | `null` |

**Example:**

`"./src"`


---

## `extensions.<name>.tags`

Tags for categorizing/filtering extensions

| Property | Value |
|----------|-------|
| **Type** | `list of string` |
| **Default** | `[ ]` |

---

## `extensions.<name>.version`

Version constraint (e.g., '^1.0.0', '~2.3', 'latest')

| Property | Value |
|----------|-------|
| **Type** | `null or string` |
| **Default** | `null` |

---


---
title: state
description: Configuration options for stackpanel.state
icon: Settings
---

# State Options

<NixOption
  path={"state.custom"}
  type={"attribute set of anything"}
  defaultValue={"{ }"}
  module={"state"}
>
Arbitrary state data contributed by modules.
Use this for module-specific state that should be serialized.
</NixOption>

<NixOption
  path={"state.devenv"}
  type={"attribute set of anything"}
  defaultValue={"{ }"}
  module={"state"}
>
Devenv-related state for serialization.
Populated by devenv integration modules (services, languages, pre-commit).

Structure:
  \{
    services = \{ available = [...]; enabled = [...]; \};
    languages = \{ available = [...]; enabled = [...]; \};
    preCommit = \{ available = [...]; enabled = [...]; \};
  \}
</NixOption>

<NixOption
  path={"state.file"}
  type={"string"}
  defaultValue={"\"stackpanel.json\""}
  module={"state"}
>
Name of the state file written to the state directory.
</NixOption>


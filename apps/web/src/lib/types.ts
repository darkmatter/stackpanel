/**
 * Re-exports auto-generated types from Nix configuration schema.
 *
 * Source of truth: nix/stackpanel/core/schema.nix
 * Generated by: nix/stackpanel/core/generate-types.sh
 *
 * To regenerate:
 *   ./nix/stackpanel/core/generate-types.sh
 */

// Re-export generated types with friendly names
export type {
  NixTypes as StackpanelConfig,
  App,
  Service,
  Network,
  Step as StepConfig,
  Paths,
  MOTD,
  Command as MOTDCommand,
} from "./generated/nix-types";

// Also export the raw generated names for consumers who prefer them
export * from "./generated/nix-types";

// =============================================================================
// Additional Types (not generated from Nix)
// =============================================================================

/**
 * SSE event types from the agent
 */
export interface ConfigChangedEvent {
  type: "config.changed";
  files: string[];
}

export interface HeartbeatEvent {
  type: "heartbeat";
  timestamp: number;
}

export type SSEEvent = ConfigChangedEvent | HeartbeatEvent;

// =============================================================================
// Data Entity Types - For .stackpanel/data/ files
// =============================================================================

/** Base response for data operations */
export interface DataResponse<T> {
  entity: string;
  exists: boolean;
  data: T | null;
}

/** Write response */
export interface WriteResponse {
  entity: string;
  path: string;
  success: boolean;
}

/** Delete response */
export interface DeleteResponse {
  entity: string;
  deleted: boolean;
  reason?: string;
}

/** List response */
export interface ListResponse {
  entities: string[];
}

// =============================================================================
// API Request/Response Types
// =============================================================================

/** Exec command request */
export interface ExecRequest {
  command: string;
  args?: string[];
  cwd?: string;
  env?: string[];
}

/** Exec command result */
export interface ExecResult {
  exit_code: number;
  stdout: string;
  stderr: string;
}

/** Nix eval request */
export interface NixEvalRequest {
  expression: string;
  file?: string;
}

/** File content response */
export interface FileContent {
  path: string;
  content: string;
  exists: boolean;
}

/** Generate command result */
export interface GenerateResult {
  success: boolean;
  output: string;
  error?: string;
}

/** Secret environment */
export type SecretEnv = "dev" | "staging" | "prod";

/** Set secret request */
export interface SetSecretRequest {
  env: SecretEnv;
  key: string;
  value: string;
}

/** Set secret result */
export interface SetSecretResult {
  path: string;
}

/** Nix data write request */
export interface NixDataWriteRequest {
  entity: string;
  data: unknown;
}

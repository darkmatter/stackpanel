# Auto-generated by StackPanel - do not edit manually
# Run 'generate-sops-config' to regenerate
#
# Rules are matched top-down; first match wins.
# Prod gets its own rule; everything else falls through to the
# dev default at the bottom.

creation_rules:
  # --- prod secrets (restricted recipients) ---
  - path_regex: prod\.yaml$
    key_groups:
      - age:
          - age1dszst59kqxrnvpkkh50ad932znnykjte0chnzuvrr4ukmuasey8s3z7xvn

  # --- group encryption keys (.enc.age files, encrypted to local AGE key) ---
  - path_regex: \.enc\.age$
    key_groups:
      - age:
          - age1hmtj026e86wm89w69vx4l0fwynxmm7dljhfpf3csn266c6qn49hshda62f

  # --- default: encrypt to the dev group key ---
  - key_groups:
      - age:
          - age1dszst59kqxrnvpkkh50ad932znnykjte0chnzuvrr4ukmuasey8s3z7xvn

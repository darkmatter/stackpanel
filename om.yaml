ci:
  default:
    stackpanel:
      dir: .
      steps:
        build:
          enable: true
        lockfile:
          enable: true
        flake-check:
          enable: true
health:
  default:
    caches:
      required:
        - "https://darkmatter.cachix.org"
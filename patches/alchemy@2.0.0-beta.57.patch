diff --git a/lib/Cloudflare/Workers/Worker.js b/lib/Cloudflare/Workers/Worker.js
index 912be17fdd1a0380471856b0c485a7f1a88dbc0b..9bf422c05917ce23cc42c7f00550720c201898c6 100644
--- a/lib/Cloudflare/Workers/Worker.js
+++ b/lib/Cloudflare/Workers/Worker.js
@@ -584,7 +584,15 @@ export const LiveWorkerProvider = () => Provider.effect(Worker, Effect.gen(funct
         const toRemove = liveAll.filter((d) => !desiredSet.has(d.hostname));
         yield* Effect.all(toRemove.map((d) => workers
             .deleteDomain({ accountId, domainId: d.id })
-            .pipe(Effect.catchTag("DomainNotFound", () => Effect.void))), { concurrency: "unbounded" });
+            // darkmatter: workers-domain DELETE returns HTTP 200 with a
+            // literal `null` body; the bundled 0.26.1 distilled client can't
+            // decode `null` against its structured DeleteDomainResponse and
+            // wraps it as CloudflareHttpError{status:200,statusText:"Schema
+            // decode failed",body:"null"} — reproduced through alchemy's own
+            // client. The delete DID happen server-side, so swallow exactly
+            // that signature; every other status (403/409/5xx/404) still
+            // fails the deploy.
+            .pipe(Effect.catchTags({ DomainNotFound: () => Effect.void, CloudflareHttpError: (e) => e.status === 200 && e.statusText === "Schema decode failed" ? Effect.void : Effect.fail(e) }))), { concurrency: "unbounded" });
         if (desired.length === 0)
             return [];
         const zoneCache = new Map();
@@ -1588,7 +1596,7 @@ export const LiveWorkerProvider = () => Provider.effect(Worker, Effect.gen(funct
                             accountId: output.accountId,
                             domainId: d.id,
                         })
-                            .pipe(Effect.catchTag("DomainNotFound", () => Effect.void)),
+                            .pipe(Effect.catchTags({ DomainNotFound: () => Effect.void, CloudflareHttpError: (e) => e.status === 200 && e.statusText === "Schema decode failed" ? Effect.void : Effect.fail(e) })),
                     ]
                     : []), { concurrency: "unbounded" });
             }
diff --git a/src/Cloudflare/Workers/Worker.ts b/src/Cloudflare/Workers/Worker.ts
index 57c3670916b66792b2a7ce5fe6461e53d938df4c..514f8ea2f108aa5bccacc1bcb19219595bcf614e 100644
--- a/src/Cloudflare/Workers/Worker.ts
+++ b/src/Cloudflare/Workers/Worker.ts
@@ -1020,7 +1020,22 @@ export const LiveWorkerProvider = () =>
             toRemove.map((d) =>
               workers
                 .deleteDomain({ accountId, domainId: d.id })
-                .pipe(Effect.catchTag("DomainNotFound", () => Effect.void)),
+                .pipe(
+                  // darkmatter: a successful workers-domain DELETE can return
+                  // a body the bundled distilled client can't decode
+                  // (CloudflareDecodeError: status 200, statusText "Schema
+                  // decode failed"). The delete already happened server-side,
+                  // so treat exactly that signature as success; every real
+                  // HTTP error still fails the deploy.
+                  Effect.catchTags({
+                    DomainNotFound: () => Effect.void,
+                    CloudflareHttpError: (e) =>
+                      e.status === 200 &&
+                      e.statusText === "Schema decode failed"
+                        ? Effect.void
+                        : Effect.fail(e),
+                  }),
+                ),
             ),
             { concurrency: "unbounded" },
           );
@@ -2375,7 +2390,14 @@ export const LiveWorkerProvider = () =>
                           domainId: d.id,
                         })
                         .pipe(
-                          Effect.catchTag("DomainNotFound", () => Effect.void),
+                          Effect.catchTags({
+                            DomainNotFound: () => Effect.void,
+                            CloudflareHttpError: (e) =>
+                              e.status === 200 &&
+                              e.statusText === "Schema decode failed"
+                                ? Effect.void
+                                : Effect.fail(e),
+                          }),
                         ),
                     ]
                   : [],

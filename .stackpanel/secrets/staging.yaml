# Staging environment secrets
# This file should be encrypted with SOPS before committing
#
# To encrypt: sops -e -i staging.yaml
# To edit:    sops staging.yaml
#
# Secrets are referenced in variables.nix as:
#   ref+sops://.stackpanel/secrets/staging.yaml#/KEY_NAME

# Example secrets (replace with real values before encrypting):
DATABASE_URL: "postgresql://user:pass@staging-db:5432/staging"

// Auto-generated by Stackpanel — do not edit manually.
// Exposes env for this app by selecting the right environment at runtime.
import * as app from "../apps/web/index";

const processEnv = typeof process === "undefined"
  ? {}
  : (process.env as Record<string, string | undefined>);

const rawEnvName = processEnv.APP_ENV || processEnv.NODE_ENV || "dev";
const envName = rawEnvName === "production" ? "prod" : rawEnvName === "development" ? "dev" : rawEnvName;

export type Env = app.dev.Env | app.prod.Env | app.staging.Env;

const env: Env = (() => {
  if (envName === "dev") return app.dev.getEnv(processEnv);
  if (envName === "prod") return app.prod.getEnv(processEnv);
  if (envName === "staging") return app.staging.getEnv(processEnv);
  return app.dev.getEnv(processEnv);
})();

export { env };

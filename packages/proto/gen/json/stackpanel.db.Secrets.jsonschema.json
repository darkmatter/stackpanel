{
  "$id": "stackpanel.db.Secrets.jsonschema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "Secrets management configuration",
  "patternProperties": {
    "^(input_directory)$": {
      "description": "Directory containing SOPS-encrypted secrets (legacy SOPS layout).\n Used when decrypting/merging YAML sources defined under environments.",
      "type": "string"
    },
    "^(master_keys)$": {
      "additionalProperties": {
        "$ref": "stackpanel.db.MasterKey.jsonschema.json"
      },
      "description": "Master keys for encrypting/decrypting secrets.\n Each secret specifies which master keys can decrypt it via the master-keys field.\n A default \"local\" key is auto-generated if no keys are configured.",
      "propertyNames": {
        "type": "string"
      },
      "type": "object"
    },
    "^(secrets_dir)$": {
      "type": "string"
    },
    "^(system_keys)$": {
      "description": "System-level AGE public keys (CI, deploy servers, etc.).\n These keys can decrypt all secrets regardless of environment restrictions.",
      "items": {
        "type": "string"
      },
      "type": "array"
    }
  },
  "properties": {
    "codegen": {
      "additionalProperties": {
        "$ref": "stackpanel.db.CodegenTarget.jsonschema.json"
      },
      "description": "Code generation targets keyed by name (e.g., typescript, go, python).\n Used to drive language-specific env/secret helpers.",
      "propertyNames": {
        "type": "string"
      },
      "type": "object"
    },
    "enable": {
      "default": false,
      "type": "boolean"
    },
    "environments": {
      "additionalProperties": {
        "$ref": "stackpanel.db.Environment.jsonschema.json"
      },
      "description": "Legacy environment-specific secrets configuration.",
      "propertyNames": {
        "type": "string"
      },
      "type": "object"
    },
    "groups": {
      "additionalProperties": {
        "$ref": "stackpanel.db.SecretsGroup.jsonschema.json"
      },
      "description": "Deprecated legacy groups metadata.",
      "propertyNames": {
        "type": "string"
      },
      "type": "object"
    },
    "inputDirectory": {
      "description": "Directory containing SOPS-encrypted secrets (legacy SOPS layout).\n Used when decrypting/merging YAML sources defined under environments.",
      "type": "string"
    },
    "masterKeys": {
      "additionalProperties": {
        "$ref": "stackpanel.db.MasterKey.jsonschema.json"
      },
      "description": "Master keys for encrypting/decrypting secrets.\n Each secret specifies which master keys can decrypt it via the master-keys field.\n A default \"local\" key is auto-generated if no keys are configured.",
      "propertyNames": {
        "type": "string"
      },
      "type": "object"
    },
    "secretsDir": {
      "type": "string"
    },
    "systemKeys": {
      "description": "System-level AGE public keys (CI, deploy servers, etc.).\n These keys can decrypt all secrets regardless of environment restrictions.",
      "items": {
        "type": "string"
      },
      "type": "array"
    }
  },
  "title": "Secrets",
  "type": "object"
}

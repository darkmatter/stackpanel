version: v2
modules:
  - path: proto
    name: buf.build/darkmatter/stackpanel
lint:
  use:
    - STANDARD
  except:
    - PACKAGE_VERSION_SUFFIX
    - PACKAGE_DIRECTORY_MATCH
breaking:
  use:
    - FILE

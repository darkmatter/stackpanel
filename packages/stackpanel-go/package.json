{
  "name": "stackpanel-go",
  "private": true,
  "scripts": {
    "deps": "go mod tidy && go mod download",
    "lint": "golangci-lint run ./...",
    "format": "gofumpt -l -w .",
    "test": "go test -v ./...",
    "build": "go build ./...",
    "dev": "air"
  }
}

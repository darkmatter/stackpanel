# Variables

Variables hold shared configuration values for your apps. Use secrets for
sensitive values that should stay encrypted.

## Tips

- Keep descriptions short and specific.
- Use computed/service variables for generated values.

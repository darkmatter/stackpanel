# Multi App Example

Monorepo example with three apps, shared services, and deploy target mapping.

## Initialize

```bash
nix flake init -t github:darkmatter/stackpanel#example-multi-app
```

## Enter shell

```bash
direnv allow
# or
nix develop --impure
```

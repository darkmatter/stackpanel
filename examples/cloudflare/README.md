# Cloudflare Example

Edge-focused example with app-level Cloudflare deployment settings.

## Initialize

```bash
nix flake init -t github:darkmatter/stack#example-cloudflare
```

## Enter shell

```bash
direnv allow
# or
nix develop --impure
```

# ==============================================================================
# users.nix
#
# Users - Team members with project access.
# Add overrides or additional users here.
# See: https://stackpanel.dev/docs/users
#
# Each entry can also live in its own file under .stack/config/users/<id>.nix
# (the haumea loader will merge them into a single users attrset).
# ==============================================================================
{
  # johndoe = {
  #   name = "John Doe";
  #   github = "johndoe";
  #   email = "john@example.com";
  # };
}

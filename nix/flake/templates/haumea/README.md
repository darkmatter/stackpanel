# Stackpanel Tree Config Project

This project uses Stackpanel with a tree-style `.stack/config/` layout.

## Getting Started

```bash
nix develop
# or, with direnv
direnv allow
```

## Configuration

Edit files under `.stack/config/` to configure Stackpanel. Each top-level option can live in its own Nix file.

# {{PACKAGE_NAME}}

> Auto-generated by Stackpanel — do not edit manually.
> Regenerate with `write-files` or restart devshell.

Shared feature-flag definitions and provider utilities for Studio.

## Quick Start

```tsx
import {
  FeatureFlagProvider,
  useFeatureFlags,
  FEATURE_FLAG_KEYS,
} from "@gen/featureflags";
```

Use `FeatureFlagProvider` once in your app shell and then read values with
`useFeatureFlags()`.

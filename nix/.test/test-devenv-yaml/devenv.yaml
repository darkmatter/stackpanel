# Test that devenv.yaml imports still work
# Run: cd nix/.test/test-devenv-yaml && devenv shell
#
# This simulates what a user would do with:
#   inputs:
#     stackpanel:
#       url: https://flakehub.com/f/darkmatter/stackpanel/*
#   imports:
#     - stackpanel/nix/modules
#
inputs:
  stackpanel:
    url: path:../../..
imports:
  - stackpanel/nix/modules

